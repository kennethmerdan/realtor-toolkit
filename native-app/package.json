{
  "name": "realtor-hub",
  "version": "1.0.0",
  "description": "Native mobile app for Realtor Hub",
  "scripts": {
    "build:android": "cap sync android && cd android && ./gradlew assembleRelease",
    "build:ios": "cap sync ios && npx cap open ios",
    "sync": "cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/ios": "^5.0.0",
    "@capacitor/android": "^5.0.0",
    "@capacitor/push-notifications": "^5.0.0",
    "@capacitor/splash-screen": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
